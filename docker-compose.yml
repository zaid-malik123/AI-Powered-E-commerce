services:

  frontend:
    build: ./frontend
    depends_on:
      - backend
    ports:
      - "3000:80"
  
  admin:
    build: ./admin
    depends_on:
      - backend
    ports:
      - "5174:80"

  backend:
    build: ./backend
    ports:
      - "4000:4000"
    env_file:
      - ./backend/.env